# Postgres
DATABASE_URL=postgresql+psycopg://farmaconsulta:changeme@localhost:5432/farmaconsulta

# JWT (generar con: openssl rand -hex 32)
JWT_SECRET=changeme
JWT_EXPIRE_MINUTES=600

# CORS: dominio exacto del frontend, sin barra final
FRONTEND_URL=http://localhost:5173

ENV=development

# Solo usados por scripts/seed_usuario.py
SEED_USERNAME=farmacia
SEED_PASSWORD=changeme
