# Variables para docker-compose.yml.
# Copiá este archivo a ".env" (mismo directorio) y completá valores propios.
# ".env" está en .gitignore: nunca lo commitees.

POSTGRES_USER=farmaconsulta
POSTGRES_PASSWORD=changeme
POSTGRES_DB=farmaconsulta

# Generar con: openssl rand -hex 32
JWT_SECRET=changeme
JWT_EXPIRE_MINUTES=600

FRONTEND_URL=http://localhost:5173
ENV=development
