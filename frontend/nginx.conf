server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA: cualquier ruta que no sea un archivo real cae en index.html,
    # para que React Router maneje el routing del lado del cliente
    location / {
        try_files $uri $uri/ /index.html;
    }
}
